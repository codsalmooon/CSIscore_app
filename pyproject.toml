[project]
name = "csi-score"
version = "0.1.0"
description = "Streamlit app for collecting and evaluating a 5-factor CSI score without Collaboration."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "streamlit>=1.35",
]

[tool.uv]
package = false

